{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys; cmd=sys.stdin.read().strip(); forbidden=['.env', '.git', 'package-lock.json', 'yarn.lock']; sys.exit(1) if any(f in cmd for f in forbidden) and ('rm ' in cmd or 'remove' in cmd) else sys.exit(0)\""
          }
        ]
      }
    ]
  }
}
